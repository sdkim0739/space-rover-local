// const completionCode = localStorage.getItem("completionCode");
// const code = document.getElementById("code");
// code.innerHTML = completionCode;

localStorage.clear(); //delte all local storage after study is over
